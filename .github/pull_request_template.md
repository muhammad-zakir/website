<!--
  Conventions: .handbook/conventions/rules/pull-requests.md. The short version:

    Title      Refresh token on 401 instead of logging out
               Imperative mood, no trailing period, ~70 chars, no issue ref.
               This becomes the merge commit subject, so it is the git
               history — not decoration.

    Not ready  Open as a draft. Never put [WIP] in the title.
-->

## Why

<!-- `Closes #12` here if this resolves an issue — nothing else closes it on merge. -->

## What changed

## How to verify

---

- [ ] Merging with **Create a merge commit**
- [ ] One `type:` label applied, matching the branch prefix
